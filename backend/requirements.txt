# ── Framework principal ──
fastapi==0.109.2
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Base de datos ──
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# ── WebSockets ──
websockets==12.0

# ── IA / Visión ──
# NOTA: mediapipe==0.10.14 requiere protobuf<5 (incompatible con tensorflow>=2.21.0)
# En Windows usar mediapipe para keypoints y tensorflow por separado
# Ver docs/INSTALACION.md para instrucciones detalladas
mediapipe==0.10.14
opencv-python-headless==4.9.0.80
numpy==1.26.4
protobuf==4.25.3

# ── TensorFlow (clasificación de señas - Fase 2) ──
tensorflow==2.21.0
# requiere: protobuf>=6.31.1 (incompatible con mediapipe)

# ── NLP ──
spacy==3.7.4

# ── Utilidades ──
Pillow==10.3.0
python-multipart==0.0.9

# ── Testing ──
pytest==8.2.0
httpx==0.27.0